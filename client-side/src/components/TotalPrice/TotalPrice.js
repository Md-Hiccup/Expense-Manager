import React from 'react';

const TotalPrice = (props) => (
    <p style={{textAlign: 'center', padding: '30px'}}><strong> TOTAL PRICE : </strong> {props.totalPrice}</p>
);

export default TotalPrice;